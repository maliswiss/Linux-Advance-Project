# ✂️ Part 4 — Text Processing & String Manipulation

Three real-world tasks where opening the file in an editor isn't an option — everything happens via command line.

## 📂 Sub-tasks

| Task | Folder | Skill |
|------|--------|-------|
| **1** | [`task1-terraform-update/`](./task1-terraform-update/) | Parse JSON → update Terraform |
| **2** | [`task2-pem-formatting/`](./task2-pem-formatting/) | One-line PEM → multi-line PEM |
| **3** | [`task3-invalid-users/`](./task3-invalid-users/) | SSH log forensics |

Each task uses different combinations of `grep`, `sed`, `awk`, `cut`, and `tr`.
